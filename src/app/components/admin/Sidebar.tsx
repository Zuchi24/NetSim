import React, { useState } from 'react'; import { useNavigate, useLocation } from 'react-router'; import { LayoutDashboard, Users, UserCircle, LogOut, ChevronDown, ChevronRight, GraduationCap } from 'lucide-react'; import { clsx, type ClassValue } from 'clsx'; import { twMerge } from 'tailwind-merge'; import { YEARS, SECTIONS } from '../../data/mock'; import { toast } from 'sonner'; function cn(...inputs: ClassValue[]) { return twMerge(clsx(inputs)); } export function Sidebar() { const navigate = useNavigate(); const location = useLocation(); const [isStudentsExpanded, setIsStudentsExpanded] = useState(true); const [expandedYear, setExpandedYear] = useState<string | null>(null); const isActive = (path: string) => location.pathname.startsWith(path); const navItems = [ { name: 'Dashboard', icon: LayoutDashboard, path: '/admin/dashboard' }, ]; const handleYearClick = (year: string) => { setExpandedYear(expandedYear === year ? null : year); navigate(`/admin/students/${encodeURIComponent(year)}`); }; const handleSectionClick = (year: string, section: string) => { navigate(`/admin/students/${encodeURIComponent(year)}/${encodeURIComponent(section)}`); }; const handleLogout = () => { localStorage.removeItem("userRole"); toast.success("Logged out successfully"); navigate("/login"); }; return ( <div className="w-64 h-screen bg-white border-r border-gray-200 flex flex-col fixed left-0 top-0 overflow-y-auto z-50"> <div className="p-6"> <div className="flex items-center gap-2 mb-8"> <div className="w-8 h-8 bg-blue-600 rounded-lg flex items-center justify-center"> <span className="text-white font-bold text-lg">N</span> </div> <h1 className="text-xl font-bold text-gray-900 tracking-tight">NetSim</h1> </div> <nav className="space-y-1"> {navItems.map((item) => ( <button key={item.name} onClick={() => navigate(item.path)} className={cn( "w-full flex items-center gap-3 px-3 py-2 rounded-lg text-sm font-medium transition-colors", isActive(item.path) ? "bg-blue-50 text-blue-600" : "text-gray-600 hover:bg-gray-50 hover:text-gray-900" )} > <item.icon size={18} /> {item.name} </button> ))} {/* Students Hierarchical Menu */} <div> <button onClick={() => { setIsStudentsExpanded(!isStudentsExpanded); if (!isStudentsExpanded) navigate('/admin/students'); }} className={cn( "w-full flex items-center justify-between px-3 py-2 rounded-lg text-sm font-medium transition-colors", isActive('/admin/students') ? "bg-blue-50 text-blue-600" : "text-gray-600 hover:bg-gray-50 hover:text-gray-900" )} > <div className="flex items-center gap-3"> <Users size={18} /> <span>Students</span> </div> {isStudentsExpanded ? <ChevronDown size={16} /> : <ChevronRight size={16} />} </button> {isStudentsExpanded && ( <div className="mt-1 ml-4 border-l border-gray-100 pl-2 space-y-1"> {YEARS.map((year) => ( <div key={year}> <button onClick={() => handleYearClick(year)} className={cn( "w-full flex items-center justify-between px-3 py-1.5 rounded-md text-xs font-medium transition-colors", location.pathname.includes(encodeURIComponent(year)) ? "text-blue-600 bg-blue-50/50" : "text-gray-500 hover:bg-gray-50 hover:text-gray-800" )} > <div className="flex items-center gap-2"> <GraduationCap size={14} /> {year} </div> {expandedYear === year ? <ChevronDown size={12} /> : <ChevronRight size={12} />} </button> {expandedYear === year && ( <div className="overflow-hidden mt-1 ml-4 border-l border-gray-100 pl-2 space-y-1"> {SECTIONS.map((section) => ( <button key={section} onClick={() => handleSectionClick(year, section)} className={cn( "w-full text-left px-3 py-1.5 rounded-md text-xs transition-colors", location.pathname.includes(encodeURIComponent(section)) ? "text-blue-600 font-medium" : "text-gray-500 hover:text-gray-800" )} > {section} </button> ))} </div> )} </div> ))} </div> )} </div> <button onClick={() => navigate('/admin/settings')} className={cn( "w-full flex items-center gap-3 px-3 py-2 rounded-lg text-sm font-medium mt-4 transition-colors", isActive('/admin/settings') ? "bg-blue-50 text-blue-600" : "text-gray-600 hover:bg-gray-50 hover:text-gray-900" )} > <UserCircle size={18} /> Profile </button> </nav> </div> <div className="mt-auto p-6 border-t border-gray-100"> <button onClick={handleLogout} className="w-full flex items-center gap-3 px-3 py-2 rounded-lg text-sm font-medium text-red-600 hover:bg-red-50 transition-colors" > <LogOut size={18} /> Logout </button> </div> </div> ); }